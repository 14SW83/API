public class Average{
	private ArrayList<Integer> list = new ArrayList<Integer>();
	public doubleCalculateAverage(ArrayList<Integer>ob){
		list = obj double sum=0,arg;
		int total = ob.size();
		for(int i=1;i<total;i++){
			size+=list.get(i);}
		avg=sum/total;
		
		return arg(0);
	}
}